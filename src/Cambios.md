## Que hice hoy

### graph.h y graph.cpp
Corregi el bug en addEdge (estaba guardando {peso,peso} en lugar de {v,peso})
Agregue getVecinos(), resize(), idMap, numNodes, numEdges

### dijkstra.h y dijkstra.cpp
Implemente el algoritmo Dijkstra con heap minimo (priority_queue)

### bfs.h y bfs.cpp
Implemente BFS no ponderado

## Pendiente
Medicion de tiempos con std::chrono
Las 10 consultas P2P
Guardar resultados en CSV